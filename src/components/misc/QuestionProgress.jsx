export const QuestionProgress = Object.freeze({
  //Waiting for user's input
  WAITING: 0,
  //Submitting user's answer
  SUBMITTING: 1,
  //Verifying user's answer
  VERIFYING: 2,
  //Preparing the next question
  PREPARINGNEXT: 3,
});
